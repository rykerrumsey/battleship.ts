import { Color } from "./types"

export const default_style = {
    bg: Color.White,
    fg: Color.Black,
    border: {
        bg: Color.White
    }
}

export const inverse_default_style = {
    fg: Color.White,
    bg: Color.Black,
    border: {
        bg: Color.Black
    }
}